/**
 * calculate an rem-value for a given pixel-size
 */
remCalc(_px, _base = 18)
  return (_px / _base)rem

/**
 * calculate an em-value for a given pixel-size
 */
emCalc(_px, _base = 18)
  return (_px / _base)em

$mainFontColor = #abb2bf
$mainFontColorHover = darken($mainFontColor, 10%)
$inputFontColor = #911C80

$blue = #004b9f
$lighterBlue =  #0069b4
$lightBlue = #338bca

$backgroundColor = #8f237f

$mediaQuerySmall = '(min-width: 600px)'
$mediaQueryBig = '(min-width: 900px)'

/* Color palette */

:root {
  --darkish-purple: #911c80;
  --ocean-blue: #0669b4;
  --peacock-blue: #024b9f;
  --white: #ffffff;
  --black: #000000;
  --dark-plum: #46003c;
}

/* Text styles */

.Text-Style {
  font-family: SourceCodePro;
  font-size: 30px;
  font-weight: bold;
  line-height: 1.27;
  text-align: center;
  color: #ffffff;
  color: var(--white);
}

.Text-Style-3 {
  font-family: SourceCodePro;
  font-size: 24px;
  font-weight: bold;
  line-height: 1.38;
  text-align: center;
  color: #ffffff;
  color: var(--white);
}

.Text-Style-4 {
  font-family: SourceCodePro;
  font-size: 20px;
  font-weight: bold;
  line-height: 1.3;
  text-align: center;
  color: #ffffff;
  color: var(--white);
}

.Text-Style-5 {
  font-family: SourceCodePro;
  font-size: 16px;
  font-weight: 500;
  line-height: 1.5;
  text-align: center;
  color: #ffffff;
  color: var(--white);
}

.Text-Style-2 {
  font-family: SourceCodePro;
  font-size: 16px;
  line-height: 1.38;
  text-align: center;
  color: #ffffff;
  color: var(--white);
}
